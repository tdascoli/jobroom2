{
    "configuration": {
        "filter": "Filter (by prefix)",
        "table": {
            "prefix": "Prefix",
            "properties": "Properties"
        },
        "title": "Configuration"
    }
}
