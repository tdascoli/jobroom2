import { TestBed } from '@angular/core/testing';
import { NgbActiveModal } from '@ng-bootstrap/ng-bootstrap';

describe('jobPublicationCancelReducer', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            providers: [NgbActiveModal]
        })
    });

    // todo: implement
});
